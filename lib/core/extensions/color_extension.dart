import 'package:flutter/material.dart';

extension ColorExtension on Color {
  /// Gera a cor secundária a partir da cor primária seguindo as regras de cores
  /// do material design 3 (MD3).
  // Color secondary() {

  // }

  /// Gera a cor terciária a partir da cor primária seguindo as regras de cores
  /// do material design 3 (MD3).
  // Color tertiary() {
  // }

  /// Gera a cor de um container a partir da cor primária seguindo as regras de
  /// cores do material design 3 (MD3).
  // Color primaryContainer() {

  // }
}
